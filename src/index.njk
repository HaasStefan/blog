---
title: NG Journal
description: NG Journal is a blog focusing on Angular, Typescript and Javascript.
keywords: ng-journal ng journal ngjournal stefan-haas stefan haas angular journal blog articles programming angular javascript js typescript ts nx ngrx rxjs webdev frontend
layout: "base.njk"
image: '/assets/ng-journal.png'
---


<div class="sm:flex sm:justify-center mt-3">
  <div class="max-w-screen-lg lg:max-w-screen-xl p-4">
    <h1 class="font-bold text-4xl mb-2">The Journal for Angular</h1>
    <h2 class="text-xl text-neutral-600 font-bold mb-6">
      Beyond the basics. Exploring strange new worlds and diving deep into enterprise topics.
    </h2>

    <section id="tags" class="flex gap-4 mb-10 flex-wrap">
      <div
        class="flex items-center gap-1 rounded-lg p-2 px-4 bg-white"
      >
        <img src="/assets/angular.png" width="35" height="35" />
        <span class="font-semibold text-gray-800">Angular</span>
      </div>

      <div
        class="flex items-center gap-1 rounded-lg p-2 px-4 bg-white"
      >
        <img src="/assets/ngrx.svg" width="35" height="35" />
        <span class="font-semibold text-gray-800">NgRx</span>
      </div>

      <div
        class="flex items-center gap-2  rounded-lg p-2 px-4 bg-white"
      >
        <img src="/assets/rxjs.png" width="30" height="30" />
        <span class="font-semibold text-gray-800">RxJS</span>
      </div>

      <div
        class="flex items-center gap-1  rounded-lg p-2 px-4 bg-white"
      >
        <img src="/assets/nx.png" height="35" width="35" />
        <span class="font-semibold text-gray-800">Nx</span>
      </div>


      <div
        class="flex items-center gap-1 rounded-lg p-2 px-4 bg-white"
      >
        ...
      </div>
    </section>

    <section id="index-email" class="rounded-lg bg-atom-dark text-white p-4 px-4 md:p-6 md:px-12 shadow-xl mb-10">
      {% include 'email.njk' %}
    </section>

    <section class="mb-6">
      <h1 class="font-bold text-3xl mb-4">Featured Articles</h1>
      <ul class="flex-auto flex flex-row flex-wrap">
        {%- for post in collections.featured | reverse -%}
        {% if collections.featured | length | threeOrMore %}
        <li class="md:basis-1/2 lg:basis-1/3 p-2">
        {% else %}
        <li class="md:basis-1/2 md:p-4 py-2 md:py-0">
        {% endif %}
{% set title = post.data.title %} {% set description =
          post.data.description %} {% set date = post.data.date %} {% set image
          = post.data.image %} {% set imageAlt = post.data.imageAlt %} {% set
          keywords = post.data.keywords %} {% set link = post.url %} {% include
          'blog-card.njk' %}
        </li>
        {% endfor %}
      </ul>


    </section>

    <section>
      <div
        class="flex justify-center items-center lg:mt-8 md:mt-6 mt-6 mb-6 px-4"
      >
        <div class="hidden md:block md:basis-1/4"></div>
        <div class="basis-full md:basis-2/4 flex">
          <a
            href="/blog"
            class="w-full text-white bg-gradient-to-br shadow-xl hover:shadow-2xl from-pink-500 to-orange-400 hover:bg-gradient-to-bl focus:ring-4 focus:outline-none focus:ring-pink-200 font-medium rounded-lg text-xl px-5 py-2.5 text-center mr-2 mb-2"
          >
            Read more...
          </a>
        </div>
        <div class="hidden md:block md:basis-1/4"></div>
      </div>
    </section>
  </div>
</div>
